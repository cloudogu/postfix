apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postfix.name" . }}-config
  labels: {{ include "postfix.labels" . | nindent 4 }}
data:
  normal: |{{ toYaml .Values.configuration.normal | nindent 4 }}
  sensitive: |
    {}